name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (full)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false

      - name: Read version from tag
        id: vars
        run: echo "VER=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Prepare package structure
        run: |
          set -e
          mkdir -p dist/ushakov.cookie
          
          # Копируем основные файлы модуля
          cp -r options.php options_conf.php include.php dist/ushakov.cookie/
          cp -r lang install dist/ushakov.cookie/
          
          cd dist
          zip -r "ushakov.cookie-${{ steps.vars.outputs.VER }}.zip" ushakov.cookie

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/ushakov.cookie-${{ steps.vars.outputs.VER }}.zip
          draft: false
          prerelease: false
